# Core dependencies
claude-agent-sdk==0.1.4

# AWS SDK (optional, for CloudWatch metrics in production)
boto3==1.40.70

# GitHub integration
PyGithub==1.59.0

# Async support (if needed by SDK)
aiofiles==23.1.0

# Additional utilities
python-dotenv==1.1.1

# OpenTelemetry API for session ID propagation
# Note: Full instrumentation provided by AWS Distro for OpenTelemetry (ADOT)
# via OTEL_PYTHON_DISTRO=aws_distro environment variable
opentelemetry-api==1.36.0

# AWS Distro for OpenTelemetry (ADOT) for trace generation
# Required for AgentCore observability - enables X-Ray trace delivery to CloudWatch
aws-opentelemetry-distro==0.13.0

# Documentation - architecture diagrams
diagrams==0.23.0
